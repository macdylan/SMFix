# SMFix 需求规格说明书

---

## 1. 项目概述

SMFix 是一个 G-code 后处理工具，将 PrusaSlicer / SuperSlicer / OrcaSlicer / Bambu Studio 输出的 G-code 文件转换为 Snapmaker 打印机兼容格式。主要解决以下问题：

- 注入打印机触摸屏所需的元数据头部（避免打印机扫描整个文件）
- 转换模型缩略图为打印机显示格式
- 优化多挤出机的预热、关闭、工具号映射等行为

### 1.1 运行形态

- 独立命令行工具，支持 macOS / Linux / Windows
- 作为 PrusaSlicer 的 Post-processing script 自动调用
- 通过 `go build` 交叉编译为 7 个平台目标

---

## 2. 支持的打印机型号

| 型号 | 标识 | 识别方式 | 头部版本 |
|------|------|----------|----------|
| Snapmaker 2 A150 | `Snapmaker 2.0 A150` | printer_model 含 `A150` 或 bed_shape 含 `160x160` | V0 |
| Snapmaker 2 A250 | `Snapmaker 2.0 A250` | printer_model 含 `A250` 或 bed_shape 含 `230x250`/`220x235` | V0 |
| Snapmaker 2 A350 | `Snapmaker 2.0 A350` | printer_model 含 `A350` 或 bed_shape 含 `320x350`/`310x350`/`320x335`/`310x335` | V0 |
| Snapmaker Artisan (A400) | `A400` | printer_model 含 `A400`/`Artisan` 或 bed_shape 含 `400x400` | V0 |
| Snapmaker J1 | `Snapmaker J1` | printer_model 含 `J1` 或 bed_shape 含 `312x200`/`324x200`/`300x200` | V1（强制） |
| Snapmaker U1 | `Snapmaker U1` | printer_model 含 `U1` 或 bed_shape 含 `270x270` | V0 |

### 2.1 挤出头类型

| 类型 | 标识 | 判定条件 |
|------|------|----------|
| 单挤出头 | `singleExtruderToolheadForSM2` | 默认值；仅一侧有耗材用量 |
| 双挤出头 | `dualExtruderToolheadForSM2` | 两侧均有耗材用量；或 printer_model 含 ` Dual`；或 printer_notes 含 `_DUAL` |

### 2.2 打印模式（IDEX 机型）

| 模式 | 触发条件 | 影响 |
|------|----------|------|
| Default | 默认 | 无特殊处理 |
| IDEX Duplication | G-code 含 `M605 S2` | 强制 Model=J1, Version=1 |
| IDEX Mirror | G-code 含 `M605 S3` | 强制 Model=J1, Version=1 |
| IDEX Backup | G-code 含 `M605 S4` | 仅设置模式标记 |

---

## 3. 输入/输出规格

### 3.1 输入

- 格式：标准 G-code 文本文件（逐行读取）
- 来源：PrusaSlicer / SuperSlicer / OrcaSlicer / Bambu Studio
- 幂等性：已处理的文件（含 `; Postprocessed by smfix` 标记）拒绝二次处理，返回错误

### 3.2 输出

- 格式：SMFix 头部 + 优化后的 G-code
- 默认覆盖输入文件，可通过 `-o` 指定输出路径
- 使用 64KB 缓冲写入

### 3.3 有效性校验

以下任一条件不满足时返回 `ErrInvalidGcode`：
- 总行数 ≥ 20（以 `; generated by` 行为起点计数）
- 能识别出打印机型号
- 至少一个挤出头有有效温度（NozzleTemperatures 不全是 -1）

---

## 4. 元数据解析

### 4.1 支持的切片软件参数格式

| 参数 | PrusaSlicer / SuperSlicer | OrcaSlicer / Bambu Studio |
|------|---------------------------|---------------------------|
| 层高 | `layer_height` | `first_layer_height` |
| 总层数 | `total_layer_number` | `total layers count` |
| 喷嘴温度 | `first_layer_temperature` | `nozzle_temperature_initial_layer` |
| 热床温度 | `first_layer_bed_temperature` | `hot_plate_temp_initial_layer` |
| 回抽长度 | `retract_length` | `retraction_length` / `filament_retraction_length` |
| 打印速度 | `max_print_speed` | `outer_wall_speed` |
| 预估时间 | `estimated printing time (normal mode)` | 同左 |

### 4.2 解析的完整参数列表

- 打印机型号（printer_model / bed_shape）
- 切片软件版本（V0 / V1，可通过 `; SNAPMAKER_GCODE_V1` 头部或 printer_notes 覆盖）
- 层高、总层数、总行数
- 喷嘴温度 [T0, T1]、热床温度 [T0, T1]
- 喷嘴直径 [T0, T1]
- 耗材类型 [T0, T1]
- 耗材用量 [mm] [T0, T1]、耗材重量 [g] [T0, T1]
- 回抽长度 [T0, T1]、换工具回抽 [T0, T1]
- 打印速度
- 打印范围 Min/Max XYZ
- 缩略图（base64 PNG）
- 预估打印时间

### 4.3 挤出头使用状态判定

- `filament used [mm]` 值 > 0 → 该挤出头已使用
- 未使用的挤出头：温度置 0、耗材类型置 `-`、回抽置 0、热床温度置 -1

### 4.4 回抽长度优先级

`filament_retract_length`（耗材级）> `retract_length`（打印机级）

---

## 5. 头部生成

### 5.1 V0 格式（A 系列打印机）

```
; Postprocessed by smfix (https://github.com/macdylan/SMFix)
;Header Start
;FAVOR:Marlin
;TIME:6666
;Filament used: {总耗材长度}m
;Layer height: {层高}
;header_type: 3dp
;tool_head: {挤出头类型}
;machine: {打印机型号}
;file_total_lines: {总行数+34}
;estimated_time(s): {预估时间×1.07}
;nozzle_temperature(°C): {T0温度}
;nozzle_0_diameter(mm): {T0喷嘴直径}
;nozzle_0_material: {T0耗材类型}
;Extruder 0 Retraction Distance: {T0回抽}
;Extruder 0 Switch Retraction Distance: {T0换工具回抽}
;nozzle_1_temperature(°C): {T1温度}
;nozzle_1_diameter(mm): {T1喷嘴直径}
;nozzle_1_material: {T1耗材类型}
;Extruder 1 Retraction Distance: {T1回抽}
;Extruder 1 Switch Retraction Distance: {T1换工具回抽}
;build_plate_temperature(°C): {有效热床温度}
;work_speed(mm/minute): {打印速度×60}
;max_x/mm/max_y/max_z/min_x/min_y/min_z: {打印范围}
;layer_number: {总层数}
;layer_height: {层高}
;matierial_weight: {总耗材重量}
;matierial_length: {总耗材长度}
;thumbnail: data:image/png;base64,{缩略图数据}
;Header End
```

### 5.2 V1 格式（J1 及新机型）

```
; Postprocessed by smfix (https://github.com/macdylan/SMFix)
;Header Start
;Version:1
;Printer:{打印机型号}
;Estimated Print Time:{预估时间}
;Lines:{总行数+27}
;Extruder Mode:{打印模式}
;Extruder 0/1 Nozzle Size/Material/Print Temperature/Retraction Distance/Switch Retraction Distance
;Bed Temperature:{有效热床温度}
;Work Range - Min/Max X/Y/Z
;Extruder(s) Used:1 或 2
;Thumbnail:{缩略图数据}
;Header End
```

---

## 6. G-code 优化功能

### 6.1 智能关闭喷嘴（GcodeFixShutoff）

**默认启用**，可通过 `-noshutoff` 禁用。

- 跟踪每个挤出头的最后使用位置
- 当切换挤出头且旧挤出头不再使用时，自动插入 `M104 S0 Tn` 关闭命令
- 关闭后出现的温度设置命令（M104/M109）被注释为无效
- 支持并行扫描（多核加速）

### 6.2 智能预热（GcodeFixPreheat）

**默认禁用**（`-nopreheat` 默认 true），需显式启用。

依赖 M73（剩余时间）指令，无 M73 时不做任何修改。

| 场景 | 行为 |
|------|------|
| 深度冻结（standby 后 ≥3 次 M73 变化） | 将 standby 温度替换为 110°C 深冻，在更早位置插入预热 |
| 短预热（standby 后 1-2 次 M73 变化） | 在适当位置插入 M104 预热到目标温度 |
| 长预热（standby 后 ≥3 次 M73 变化） | 在更早位置插入预热 |
| 冷却移除（standby 后 <1 次 M73 变化） | 直接删除 standby 命令 |
| 重复温度请求 | 注释为 `already requested temp` |
| 重复温度等待 | 首次保留，后续注释为 `already stabilized temp` |

### 6.3 工具号重映射（GcodeReplaceToolNum）

**默认启用**，可通过 `-noreplacetool` 禁用。

Snapmaker A系列/J1 最多支持 2 个喷嘴（T0/T1），U1 机型则有 4 个喷嘴(T0-T3)，但切片软件可配置多材料（T0-T63）。

| 输入 | 输出 |
|------|------|
| T0 | T0 |
| T1 | T1 |
| T2 | T0 |
| T3 | T1 |
| Tn | T(n%2) |

同步重映射：
- M104/M109 的 T 参数
- M106/M107 的 P 参数（风扇）
- M301/M303 的 E 参数
- 注释中的耗材用量、耗材类型、温度等参数（按实际使用的工具号重排）

支持并行处理（多核加速），已加 mutex 保护共享变量。注意，U1 机型不要对工具号重新映射。

### 6.4 Orca 换料清理（GcodeFixOrcaToolUnload）

**始终启用**，无开关。

在 `; CP TOOLCHANGE START` 到 `; CP TOOLCHANGE END` 区间内，移除缺少工具号的 M104 命令（无效指令）。

### 6.5 已禁用功能

| 功能 | 状态 | 原因 |
|------|------|------|
| GcodeTrimLines | 代码保留，调用已注释 | — |
| GcodeReinforceTower | 代码保留，调用已注释 | Orca 已内置 Rib tower |

### 6.6 预处理

- 过滤 `G4 S0`（无意义等待）
- 单遍处理：去除特殊字符（\n\t\r）、合并连续空格、裁剪首尾空白

---

## 7. 缩略图处理

- 从 G-code 注释中提取 `; thumbnail begin WxH SIZE` ... `; thumbnail end` 区间数据
- 多个缩略图时取最后一个（最大尺寸）
- 转换为 `data:image/png;base64,...` 格式
- PrusaSlicer 需配置 `G-code thumbnails: 220x124`

---

## 8. 命令行接口

```
smfix [flags] <input.gcode>

Flags:
  -o string          输出路径（默认覆盖输入文件）
  -notrim            不裁剪行内空格（当前未生效）
  -noshutoff         禁用智能关闭喷嘴
  -nopreheat         禁用智能预热（默认禁用）
  -noreplacetool     禁用工具号重映射
```

---

## 9. 构建与发布

### 9.1 交叉编译目标

| 目标 | GOOS/GOARCH |
|------|-------------|
| darwin-arm64 | macOS Apple Silicon |
| darwin-amd64 | macOS Intel |
| linux-amd64 | Linux x86_64 |
| linux-arm7 | Linux ARMv7 |
| linux-arm6 | Linux ARMv6 |
| win64 | Windows x86_64 |
| win32 | Windows x86 |

### 9.2 CI/CD

- GitHub Actions，tag 触发（`v*.*`）
- 自动构建全部平台 → 打包 zip → 创建 Draft Release

### 9.3 可选功能

- `pprof` build tag：启用 CPU/Memory profiling（`make pprof`）

---

## 10. 测试覆盖

| 模块 | 覆盖率 | 测试内容 |
|------|--------|----------|
| ParseParams | 99.1% | PS/BBS 格式解析、3 种错误路径、6 种 bed_shape 识别、6 种 printer_model 识别、3 种打印模式、单/双挤出头判定、版本覆盖、回抽回退、缩略图提取、行数重置、有效温度 |
| GcodeFixShutoff | 100% | 关闭插入、后续温度命令注释化 |
| GcodeFixPreheat | 98.3% | 深冻、短/长预热、冷却移除、重复温度处理 |
| GcodeReplaceToolNum | 100% | T/M 指令替换、注释参数重排（4 种场景） |
| GcodeFixOrcaToolUnload | 100% | TOOLCHANGE 区间无效 M104 移除 |
| Gcode/GcodeBlock 核心 | ~95% | 解析、格式化、类型转换、工具号提取 |
| **总计** | **83.5%** | |

---

## 11. 已知约束与边界

1. A系列/J1 仅支持 2 个挤出头（T0/T1），多工具通过 `%2` 映射。U1 不要重映射。
2. J1 仅支持 V1 头部格式
3. IDEX Duplication/Mirror 模式强制识别为 J1
4. 预估时间乘以 1.07 系数（V0 头部）
5. 缩略图仅支持 PNG base64 编码
6. 输入文件必须是 UTF-8 纯文本 G-code
7. 不支持二次处理（幂等性保护）
